# Plantilla de variables de entorno.
#
# Copia este archivo a uno nuevo llamado .env (con punto al inicio):
#
#   Mac / Linux:  cp env.example.txt .env
#   Windows:      copy env.example.txt .env
#
# Luego abre el .env y pon TU contraseña de MySQL.
# El .env nunca se sube a GitHub: ya está en el .gitignore.

DB_NAME=monitoreo_red
DB_USER=root
DB_PASSWORD=tu_contraseña
DB_HOST=localhost
DB_PORT=3306

DEBUG=True
DEFAULT_FROM_EMAIL=alertas@monitoreo.local
